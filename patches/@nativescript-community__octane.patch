diff --git a/dist/config.d.ts b/dist/config.d.ts
index 57293a9396a0a4428bbb0571974149633e7f8d61..ed0c070d09ff2822a25a898656ababa73f20237c 100644
--- a/dist/config.d.ts
+++ b/dist/config.d.ts
@@ -15,6 +15,15 @@ export declare const nativeScriptRenderer: {
     readonly intrinsics: "@nativescript-community/octane";
     /** `<label>text</label>` lowers to a `#text` host the driver folds into the parent's `text`. */
     readonly text: "host";
+    /**
+     * DOM has no business in a native target: fail the transform on unbound
+     * DOM globals or DOM-side octane imports. Globals that exist on NS
+     * (fetch, WebSocket, crypto, btoa, matchMedia, navigator) are allowed.
+     */
+    readonly validation: {
+        readonly forbiddenGlobals: readonly string[];
+        readonly forbiddenImports: readonly string[];
+    };
 };
 export interface NativeScriptRenderersOptions {
     /** Glob of the modules the renderer compiles. Defaults to `src/**\/*.tsx`. */
diff --git a/dist/config.js b/dist/config.js
index c94a76e6501ad85fc4f2ae57f52365747c8d07d2..6d9551d4fc47cbf68ac4e4aa40de7de3f7dcbe10 100644
--- a/dist/config.js
+++ b/dist/config.js
@@ -15,6 +15,26 @@ export const nativeScriptRenderer = {
     intrinsics: '@nativescript-community/octane',
     /** `<label>text</label>` lowers to a `#text` host the driver folds into the parent's `text`. */
     text: 'host',
+    /**
+     * DOM has no business in a native target: fail the transform on unbound
+     * DOM globals or DOM-side octane imports. Globals that exist on NS
+     * (fetch, WebSocket, crypto, btoa, matchMedia, navigator) are allowed.
+     * Validation runs only in the compile pipeline — plain `.ts` helpers
+     * under a rule are unchecked (a no-DOM-lib tsconfig is the backstop).
+     */
+    validation: {
+        forbiddenGlobals: [
+            'document', 'window', 'HTMLElement', 'Node',
+            'localStorage', 'sessionStorage', 'DOMParser',
+            'MutationObserver', 'ResizeObserver',
+            'XMLHttpRequest', 'alert', 'confirm',
+        ],
+        forbiddenImports: [
+            'octane/dom-bindings', 'octane/dom-binding-program',
+            'octane/hydration', 'octane/hydration/streamed-signals',
+            'react-dom', 'react-dom/client',
+        ],
+    },
 };
 /**
  * The renderer registry `@octanejs/vite-plugin` reads. The renderer is scoped
diff --git a/dist/driver.js b/dist/driver.js
index 2aaac742c065660e0c195588a7553103e7f28ff1..53bc8d9633db0058f4a603b828ba569df8799af8 100644
--- a/dist/driver.js
+++ b/dist/driver.js
@@ -1,9 +1,47 @@
-import { ActionBar, ContentView, FormattedString, LayoutBase, Span, TextBase, View, unsetValue, } from '@nativescript/core';
+import { ActionBar, ContentView, FormattedString, LayoutBase, ListView, ObservableArray, Span, TextBase, View, unsetValue, } from '@nativescript/core';
 import { createUniversalRoot, } from 'octane/universal/native';
 import { NATIVESCRIPT_RENDERER_ID } from './config.js';
 import { ELEMENTS, EVENT_PROP, eventNameFor, onElementReplaced, } from './elements.js';
 /** `#text` nodes carry no view; the driver folds them into the parent's `text`. */
 const TEXT = '#text';
+/**
+ * `items` on `<listview>` is driver-managed: plain arrays are adapted to an
+ * ObservableArray the driver owns; a changed reference splices the adapter
+ * and schedules `refresh()` so the next layout pass rebinds cells. Passing
+ * an ObservableArray opts out (consumer-managed updates).
+ */
+const managedListItems = new WeakMap();
+/**
+ * Any prop write can echo back as a `<prop>Change[d]` event — `text` →
+ * `textChange`, `checked` → `checkedChange`, `selectedIndex` →
+ * `selectedIndexChanged`. A driver-initiated write records the prop name per
+ * view and drops its own echo so controlled props don't round-trip.
+ */
+const pendingPropWrites = new WeakMap();
+function setManagedItems(lv, value) {
+    if (value instanceof ObservableArray) {
+        managedListItems.delete(lv);
+        lv.items = value;
+        return;
+    }
+    const next = Array.isArray(value) ? value : [];
+    const rec = managedListItems.get(lv);
+    if (rec === undefined) {
+        const items = new ObservableArray(next);
+        managedListItems.set(lv, { array: items, ref: next });
+        lv.items = items;
+        return;
+    }
+    if (rec.ref === next)
+        return;
+    rec.ref = next;
+    rec.array.splice(0, rec.array.length, ...next);
+    // The splice queues an itemLoading wave that can run before this render
+    // commits; refresh() on a microtask forces a fresh wave with the new
+    // data once the write has landed.
+    Promise.resolve().then(() => lv.refresh());
+}
+
 function expect(container, id) {
     const node = container.nodes.get(id);
     if (node === undefined)
@@ -71,7 +109,25 @@ function setProp(view, name, value) {
         applyStyle(view, value);
         return;
     }
+    if (name === 'items' && view instanceof ListView) {
+        setManagedItems(view, value);
+        return;
+    }
+    if (view[name] === value)
+        return;
+    // Record the write so its `<name>Change[d]` echo is dropped.
+    let pending = pendingPropWrites.get(view);
+    if (pending === undefined)
+        pendingPropWrites.set(view, (pending = new Set()));
+    pending.add(name);
     view[name] = value;
+    // The echo fires synchronously on assignment on iOS; clear on a
+    // microtask so a same-tick user event isn't swallowed.
+    Promise.resolve().then(() => {
+        pending.delete(name);
+        if (pending.size === 0)
+            pendingPropWrites.delete(view);
+    });
 }
 function applyStyle(view, value) {
     if (value == null || value === unsetValue)
@@ -238,6 +294,13 @@ function setEvent(container, node, type, listener) {
         const root = container.root;
         if (root === null)
             return;
+        const echoProp = type.endsWith('Changed')
+            ? type.slice(0, -7)
+            : type.endsWith('Change')
+                ? type.slice(0, -6)
+                : null;
+        if (echoProp !== null && pendingPropWrites.get(view)?.has(echoProp))
+            return;
         const fire = () => {
             try {
                 root.eventScope(listener.priority, () => root.dispatchEvent(listener.id, data));
