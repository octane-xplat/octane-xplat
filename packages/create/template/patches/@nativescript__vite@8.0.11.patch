diff --git a/hmr/server/core-bundle.js b/hmr/server/core-bundle.js
index 993097b9d861a2a7c477f4e664213efec3eb6293..7a9b95180cbd47e1d9c71180bc4582a450333a7c 100644
--- a/hmr/server/core-bundle.js
+++ b/hmr/server/core-bundle.js
@@ -607,17 +607,32 @@ export function buildCoreMainShimCode() {
  * class/function values. `exportNames` comes from the same disk-based export
  * discovery the per-module bridge relies on (collectStaticExportNamesFromFile).
  */
-const RESERVED_EXPORT_NAMES = new Set(['default', 'break', 'case', 'catch', 'class', 'const', 'continue', 'debugger', 'delete', 'do', 'else', 'enum', 'export', 'extends', 'false', 'finally', 'for', 'function', 'if', 'import', 'in', 'instanceof', 'new', 'null', 'return', 'super', 'switch', 'this', 'throw', 'true', 'try', 'typeof', 'var', 'void', 'while', 'with', 'let', 'static', 'yield', 'await', 'eval', 'arguments', '__ns_core_sub_ns__']);
+// `default` is emitted separately (below); `__ns_core_sub_ns__` is this shim's
+// own local — a module exporting that name can't collide with it.
+const NON_DECLARABLE_EXPORT_NAMES = new Set(['default', '__ns_core_sub_ns__']);
+// Reserved words can't be declaration bindings (`export const enum` is a
+// syntax error), but they ARE legal export names — the specifier position in
+// `export { x as name }` accepts any IdentifierName. Emit those through a
+// temp + alias so a reserved-word export (zod's `enum`/`catch`/… shapes this
+// same code path) still reaches the bridged namespace.
+const RESERVED_EXPORT_NAMES = new Set(['break', 'case', 'catch', 'class', 'const', 'continue', 'debugger', 'delete', 'do', 'else', 'enum', 'export', 'extends', 'false', 'finally', 'for', 'function', 'if', 'import', 'in', 'instanceof', 'new', 'null', 'return', 'super', 'switch', 'this', 'throw', 'true', 'try', 'typeof', 'var', 'void', 'while', 'with', 'let', 'static', 'yield', 'await', 'eval', 'arguments']);
 export function buildCoreSubShimCode(sub, exportNames, hasOwnDefaultExport = false) {
     const canonical = normalizeCoreSub(sub);
     const registryKey = `@nativescript/core/${canonical}`;
-    const names = Array.from(new Set(exportNames)).filter((n) => /^[A-Za-z_$][\w$]*$/.test(n) && !RESERVED_EXPORT_NAMES.has(n));
+    const names = Array.from(new Set(exportNames)).filter((n) => /^[A-Za-z_$][\w$]*$/.test(n) && !NON_DECLARABLE_EXPORT_NAMES.has(n));
     const lines = [];
     lines.push(`/* @nativescript/core bridge (bundle mode) — sub: ${canonical} */`);
     lines.push(`import ${JSON.stringify(CORE_BUNDLE_PATH)};`);
     lines.push(`const __ns_core_sub_ns__ = (globalThis.__NS_CORE_MODULES__ || {})[${JSON.stringify(registryKey)}];`);
     lines.push(`if (!__ns_core_sub_ns__) { throw new Error(${JSON.stringify(`[ns-core] core bundle registry missing '${registryKey}' — restart the dev server or set NS_CORE_PER_MODULE=1`)}); }`);
+    let aliasIdx = 0;
     for (const name of names) {
+        if (RESERVED_EXPORT_NAMES.has(name)) {
+            const alias = `__ns_core_alias_${aliasIdx++}`;
+            lines.push(`const ${alias} = __ns_core_sub_ns__[${JSON.stringify(name)}];`);
+            lines.push(`export { ${alias} as ${name} };`);
+            continue;
+        }
         lines.push(`export const ${name} = __ns_core_sub_ns__.${name};`);
     }
     // Deep-subpath contract (see isDeepCoreSubpath / per-module bridge): a sub
diff --git a/hmr/server/deps-bundle.js b/hmr/server/deps-bundle.js
index 50e5abddddf80b35044d45eb108a64dd175bf6e7..799c7257a8a730515633f19f6221ad26ff8678eb 100644
--- a/hmr/server/deps-bundle.js
+++ b/hmr/server/deps-bundle.js
@@ -219,8 +219,37 @@ function isPackageRootSpec(spec) {
         return false;
     return pkgSub.split('/').length === (pkgSub.startsWith('@') ? 2 : 1);
 }
-function resolveSpecToFile(spec, projectRoot, workspaceRoot, platform) {
+/**
+ * `/node_modules/<pkg>/<sub...>` → the exports-mapped file on disk
+ * (`rouzer/http` → `rouzer/dist/http.js`). Without this, non-root specs fall
+ * back to `resolveId` and land on Vite's dep-optimizer artifacts under
+ * `.vite/deps/` — flat bundles whose generated shared chunks
+ * (`foo-<hash>.js`) this pipeline cannot serve to the device.
+ */
+function resolvePackageSubpathFromSpecRoots(spec, roots, platform) {
+    const pkgSub = spec.replace(/^\/node_modules\//, '');
+    const parts = pkgSub.split('/');
+    const nameSegs = parts[0].startsWith('@') ? 2 : 1;
+    if (parts.length <= nameSegs)
+        return null;
+    const subpath = parts.slice(nameSegs).join('/');
+    for (const root of roots) {
+        const pkgDir = path.join(root, 'node_modules', ...parts.slice(0, nameSegs));
+        if (!existsSync(path.join(pkgDir, 'package.json')))
+            continue;
+        const resolved = resolvePackageSubpathEntry(pkgDir, subpath, platform);
+        if (resolved)
+            return resolved;
+    }
+    return null;
+}
+export function resolveSpecToFile(spec, projectRoot, workspaceRoot, platform) {
     const roots = [projectRoot, ...(workspaceRoot && path.resolve(workspaceRoot) !== path.resolve(projectRoot) ? [workspaceRoot] : [])];
+    // `.vite/` is Vite's dep-optimizer store — generated artifacts with
+    // content-hashed names, not package files. Resolving them here would mint
+    // deps-bundle entries/shims for files whose names churn per optimization.
+    if (spec.includes('/.vite/'))
+        return null;
     // Package roots honor the declared entry (exports/module/main) FIRST —
     // matching esbuild's resolution of bare imports inside the bundle. The
     // index fan-out would otherwise pick stray root files like a shipped
@@ -236,6 +265,12 @@ function resolveSpecToFile(spec, projectRoot, workspaceRoot, platform) {
         if (abs)
             return abs;
     }
+    // Non-root specs (`rouzer/http`, `qubu/sqlite`): consult the package's
+    // exports map so the real dist file wins — the alternative downstream is
+    // the dep optimizer's `.vite/deps` artifact, whose sibling chunks 404.
+    const subpathEntry = resolvePackageSubpathFromSpecRoots(spec, roots, platform);
+    if (subpathEntry)
+        return subpathEntry;
     return isPackageRootSpec(spec) ? null : resolvePackageRootEntry(spec, roots, platform);
 }
 // Never bundled: `@nativescript/core` belongs to the /ns/core bridge realm,
@@ -869,7 +904,15 @@ export function collectDepsModuleExportInfo(absPath, platform, seen = new Set())
     names.delete('default');
     return { names: Array.from(names).sort(), hasDefault: hasExistingDefaultExport(code) };
 }
-const RESERVED_EXPORT_NAMES = new Set(['default', 'break', 'case', 'catch', 'class', 'const', 'continue', 'debugger', 'delete', 'do', 'else', 'enum', 'export', 'extends', 'false', 'finally', 'for', 'function', 'if', 'import', 'in', 'instanceof', 'new', 'null', 'return', 'super', 'switch', 'this', 'throw', 'true', 'try', 'typeof', 'var', 'void', 'while', 'with', 'let', 'static', 'yield', 'await', 'eval', 'arguments', '__ns_dep_ns__']);
+// `default` is emitted separately (below); `__ns_dep_ns__` is this shim's own
+// local — a package exporting that name can't collide with it.
+const NON_DECLARABLE_EXPORT_NAMES = new Set(['default', '__ns_dep_ns__']);
+// Reserved words can't be declaration bindings (`export const enum` is a
+// syntax error), but they ARE legal export names — the specifier position in
+// `export { x as name }` accepts any IdentifierName. Emit those through a
+// temp + alias so packages like zod (`enum`, `catch`, `function`, …) keep
+// their full surface on the bridged namespace.
+const RESERVED_EXPORT_NAMES = new Set(['break', 'case', 'catch', 'class', 'const', 'continue', 'debugger', 'delete', 'do', 'else', 'enum', 'export', 'extends', 'false', 'finally', 'for', 'function', 'if', 'import', 'in', 'instanceof', 'new', 'null', 'return', 'super', 'switch', 'this', 'throw', 'true', 'try', 'typeof', 'var', 'void', 'while', 'with', 'let', 'static', 'yield', 'await', 'eval', 'arguments']);
 /**
  * Shim served at `/ns/m/node_modules/...` for a bundled module. The static
  * bundle import guarantees the registry is populated before the body reads it
@@ -880,13 +923,20 @@ const RESERVED_EXPORT_NAMES = new Set(['default', 'break', 'case', 'catch', 'cla
 export function buildDepsShimCode(key, exportNames, hasOwnDefaultExport) {
     // JS_IDENTIFIER_RE (not ASCII \w): Angular exports `ɵɵdefineInjectable`-style
     // names — dropping them here breaks every `import * as i0` consumer.
-    const names = Array.from(new Set(exportNames)).filter((n) => JS_IDENTIFIER_RE.test(n) && !RESERVED_EXPORT_NAMES.has(n));
+    const names = Array.from(new Set(exportNames)).filter((n) => JS_IDENTIFIER_RE.test(n) && !NON_DECLARABLE_EXPORT_NAMES.has(n));
     const lines = [];
     lines.push(`/* ns-deps bridge (bundle mode) — ${key} */`);
     lines.push(`import ${JSON.stringify(DEPS_BUNDLE_PATH)};`);
     lines.push(`const __ns_dep_ns__ = (globalThis.__NS_DEPS_MODULES__ || {})[${JSON.stringify(key)}];`);
     lines.push(`if (!__ns_dep_ns__) { throw new Error(${JSON.stringify(`[ns-deps] deps bundle registry missing '${key}' — restart the dev server or set NS_DEPS_PER_MODULE=1`)}); }`);
+    let aliasIdx = 0;
     for (const name of names) {
+        if (RESERVED_EXPORT_NAMES.has(name)) {
+            const alias = `__ns_dep_alias_${aliasIdx++}`;
+            lines.push(`const ${alias} = __ns_dep_ns__[${JSON.stringify(name)}];`);
+            lines.push(`export { ${alias} as ${name} };`);
+            continue;
+        }
         lines.push(`export const ${name} = __ns_dep_ns__.${name};`);
     }
     if (hasOwnDefaultExport) {
diff --git a/hmr/server/device-transform-helpers.js b/hmr/server/device-transform-helpers.js
index 9364ce9942b84fffd0e3a6eb6700d8253fa66e7e..93ee2bf2570303277d7b4350018b00fbbb31e2a4 100644
--- a/hmr/server/device-transform-helpers.js
+++ b/hmr/server/device-transform-helpers.js
@@ -81,12 +81,18 @@ function bareSpecifierFromFlatDepPath(depPath) {
         }
         catch { }
     }
-    const bareSpecifier = resolved || candidates[0];
+    // Unverified decode → NOT a package: `depPath` is a generated optimizer
+    // artifact (a hash-named shared chunk like `http-Dm_BQhab.js`, whose
+    // base64url '_' decodes to a bogus '/'). Returning null lets the caller
+    // keep the real `.vite/deps` URL instead of emitting a garbage specifier.
+    if (!resolved) {
+        return null;
+    }
     if (!warnedVendorMisses.has(depPath)) {
         warnedVendorMisses.add(depPath);
-        console.warn(`[ns-vite] vendor-manifest miss: prebundled dep '${depPath}' is not in the vendor bundle — serving '${bareSpecifier}' over HTTP via /ns/m/node_modules/${resolved ? '' : ' (could not verify the package on disk; the specifier is a best-effort decode of the flattened id)'}.`);
+        console.warn(`[ns-vite] vendor-manifest miss: prebundled dep '${depPath}' is not in the vendor bundle — serving '${resolved}' over HTTP via /ns/m/node_modules/${resolved}.`);
     }
-    return bareSpecifier;
+    return resolved;
 }
 function rewriteVitePrebundleImportsForDevice(code, preserveVendorImports) {
     const imports = collectTopLevelImportRecords(code);
@@ -111,6 +117,15 @@ function rewriteVitePrebundleImportsForDevice(code, preserveVendorImports) {
             if (bareSpecifier) {
                 replacement = imp.text.replace(source, bareSpecifier);
             }
+            else {
+                // Generated optimizer chunk (hash-named sibling of an
+                // optimized entry) — not a package and not decodable to one.
+                // Route the real artifact URL back through the bridge: the
+                // /ns/m route serves it from the dep-optimizer store (the
+                // `?v` stamp rides along as forcedVer) or emits an
+                // actionable optimizeDeps.exclude error.
+                replacement = imp.text.replace(source, `/ns/m/node_modules/.vite/deps/${depPath}`);
+            }
         }
         edits.push({
             start: imp.start,
diff --git a/hmr/server/websocket-ns-m-request.js b/hmr/server/websocket-ns-m-request.js
index b54aefcc839e95906d76d02e522c052a706ca76f..d49159de94ba05dcfc372aa7a3c513e0f5c0a0d3 100644
--- a/hmr/server/websocket-ns-m-request.js
+++ b/hmr/server/websocket-ns-m-request.js
@@ -1,6 +1,9 @@
 import * as path from 'path';
+import { realpathSync } from 'fs';
 import { filterExistingNodeModulesTransformCandidates, getBlockedDeviceNodeModulesReason, resolveCandidateFilePath, tryReadRawExplicitJavaScriptModule } from './websocket-module-specifiers.js';
 import { collapseLegacyNsMTags } from './websocket-ns-m-paths.js';
+import { resolveSpecToFile } from './deps-bundle.js';
+import { resolvePlatform } from '../../helpers/cli-flags.js';
 function normalizeAbsoluteFilesystemSpec(spec, serverRoot) {
     const toPosix = (value) => value.replace(/\\/g, '/');
     const rootPosix = toPosix(serverRoot);
@@ -93,6 +96,29 @@ export function createNsMRequestContext(requestUrl, serverRoot, appVirtualWithSl
         return { kind: 'next' };
     }
 }
+/**
+ * Canonical request URL for a resolved node_modules file. Project-root files
+ * keep the `/node_modules/...` form so module identity matches the /ns/m
+ * spec space; files outside the project root (hoisted/workspace) use the
+ * `/@fs` form Vite can load directly. Symlinked workspace-source packages
+ * resolve to their realpath so the TS transform applies — same policy as
+ * filterExistingNodeModulesTransformCandidates.
+ */
+function nodeModulesFileToRequestUrl(absPath, serverRoot) {
+    try {
+        const real = realpathSync(absPath);
+        if (real !== absPath && !real.split(path.sep).includes('node_modules')) {
+            return `/@fs${real.replace(/\\/g, '/')}`;
+        }
+    }
+    catch { }
+    const rootPosix = path.resolve(serverRoot).replace(/\\/g, '/').replace(/\/+$/, '');
+    const absPosix = absPath.replace(/\\/g, '/');
+    if (absPosix.startsWith(`${rootPosix}/`)) {
+        return absPosix.slice(rootPosix.length);
+    }
+    return `/@fs${absPosix}`;
+}
 async function tryTransformRequest(transformRequest, url, timeoutMs) {
     try {
         return await transformRequest(url, timeoutMs);
@@ -146,6 +172,38 @@ export async function resolveNsMTransformedModule(options) {
             return { transformed, resolvedCandidate };
         }
     }
+    // Optimizer-artifact requests (`/node_modules/.vite/deps/<file>`): flat
+    // entries and hash-named shared chunks live in the dep-optimizer's store
+    // (often memory-only), keyed by the `?v=<browserHash>` stamp Vite emits —
+    // captured inbound as `forcedVer`. Replay it so Vite resolves the artifact
+    // as-is; a miss falls through to the route's transform-miss branch, which
+    // names optimizeDeps.exclude as the fix.
+    if (spec.includes('/.vite/deps/')) {
+        const depUrl = context.forcedVer ? `${spec}?v=${context.forcedVer}` : spec;
+        transformed = await tryTransformRequest(transformRequest, depUrl, timeoutMs);
+        if (transformed?.code) {
+            return { transformed, resolvedCandidate: depUrl };
+        }
+        return { transformed: null, resolvedCandidate };
+    }
+    // Bare or subpath node_modules specs (`rouzer/http`, `qubu/sqlite`) that
+    // survive the on-disk candidate fan-out would otherwise hit `resolveId`
+    // and land on Vite's dep-optimizer output: `/node_modules/.vite/deps/
+    // <flat>.js` bundles whose generated sibling chunks (`foo-<hash>.js`)
+    // this pipeline cannot serve — and whose flattened ids decode to bogus
+    // bare specifiers downstream (`http-Dm/BQhab`). Prefer the real package
+    // file (exports-map aware) so the device gets the same per-module source
+    // an `optimizeDeps.exclude` entry produces.
+    if (spec.includes('/node_modules/')) {
+        const realFile = resolveSpecToFile(spec.slice(spec.lastIndexOf('/node_modules/')), serverRoot, workspaceRoot, resolvePlatform() || 'ios');
+        const realUrl = realFile ? nodeModulesFileToRequestUrl(realFile, serverRoot) : null;
+        if (realUrl) {
+            transformed = await tryTransformRequest(transformRequest, realUrl, timeoutMs);
+            if (transformed?.code) {
+                return { transformed, resolvedCandidate: realUrl };
+            }
+        }
+    }
     const resolvedId = await resolveId(spec);
     if (resolvedId) {
         transformed = await tryTransformRequest(transformRequest, resolvedId, timeoutMs);
diff --git a/hmr/server/websocket-ns-m.js b/hmr/server/websocket-ns-m.js
index 8eeb51ab59497e7f775da3117c4207e05f1814c7..346b676fbb67ce6bebe81cc379fdbe0d0aeac876 100644
--- a/hmr/server/websocket-ns-m.js
+++ b/hmr/server/websocket-ns-m.js
@@ -265,6 +265,30 @@ export function registerNsModuleServerRoute(server, options) {
             catch { }
             // If transformRequest failed, handle bare-module vendor shims for 'vue' and 'pinia'
             if (!transformed?.code) {
+                // Dep-optimizer artifacts (`.vite/deps` entries + generated
+                // shared chunks) aren't package files — when one reaches the
+                // bridge unserved, the fix is keeping that dep out of
+                // optimizeDeps. Name the optimized set so the on-device
+                // error is actionable instead of a generic transform miss.
+                if (spec.includes('/.vite/deps/')) {
+                    const optimizedIds = [];
+                    try {
+                        for (const env of Object.values(server.environments || {})) {
+                            const meta = env?.depsOptimizer?.metadata;
+                            for (const record of [meta?.optimized, meta?.discovered]) {
+                                if (record)
+                                    optimizedIds.push(...Object.keys(record));
+                            }
+                        }
+                    }
+                    catch { }
+                    const hint = optimizedIds.length
+                        ? ` Currently optimized: ${Array.from(new Set(optimizedIds)).join(', ')} — add the package that pulls in this chunk to optimizeDeps.exclude in the native vite config (or restart with NS_DEPS_PER_MODULE=1).`
+                        : ' Add the importing package to optimizeDeps.exclude in the native vite config.';
+                    res.statusCode = 404;
+                    res.end(`throw new Error(${JSON.stringify(`[ns/m] Vite optimizeDeps artifact '${spec}' is not servable on device — its shared chunks are generated files outside the package tree.${hint}`)});\nexport {};\n`);
+                    return;
+                }
                 const bare = spec.startsWith('/') ? spec.slice(1) : spec;
                 const isBare = bare && !bare.includes('/') && !/\.(ts|tsx|js|jsx|mjs|mts|cts|vue)$/i.test(bare);
                 if (isBare && (bare === 'vue' || bare === 'nativescript-vue' || bare === 'pinia')) {
