<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev only: the Vite HMR dev server is reached over cleartext HTTP on the
  loopback host (127.0.0.1 via `adb reverse`) or the emulator NAT alias
  (10.0.2.2). Android blocks cleartext by default on targetSdk >= 28, which
  fails the module graph fetch at boot; permit it for those hosts only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
