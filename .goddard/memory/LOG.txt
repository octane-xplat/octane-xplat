2026-09-22 Octane renderer ownership is per-file at compile time (`nativeScriptRenderers` include glob): a file speaks one element vocabulary, so leaf components must split into `*.web.tsrx` / `*.native.tsrx` — `Platform.select` can't mix intrinsics in one file's JSX.
2026-09-22 Hook-containing modules must live inside the renderer include glob (`.tsx`/`.tsrx`); hooks imported from plain `.ts` bind a second Octane runtime whose dispatcher never activates — the two-copies-of-React bug repackaged.
2026-09-22 Reference app: `NathanWalker/ns-octane` — ChatGPT-style client (streaming markdown, ui-drawer `translationFunction`, Tailwind v4 via `@nativescript/tailwind`, second Octane root in a `UISheetPresentationController`); its README reads as the port's design doc.
2026-09-22 Planned leaf-resolution chain `*.ios.tsrx` → `*.native.tsrx` → `*.tsrx` via Vite resolver plugin; needs two tsconfigs for per-target `jsxImportSource`; open check: whether the include glob covers `.tsrx`, not just `.tsx`.
2026-09-22 User named One (onestack.dev) and Flutter as prior art for the cross-platform primitives/styling layer; RN/RNW/Tamagui are the comparable precedents.
2026-09-22 2026-09-21 Modal is the sharpest web↔native seam: a native modal is a second Octane root (`showModal`), so context and portals don't cross — modal APIs must be `{open, params}`-shaped, treating content as a screen, not children.
2026-09-22 2026-09-21 `List` primitive takes `items`+`renderItem`, never children: NS `ListView` item templates don't reconcile like DOM children — locked in as decision #16.
2026-09-22 2026-09-21 NS toolchain already resolves `.ios.`/`.android.` filename suffixes natively; our leaf-resolution scheme is a superset and precedence must stay aligned so the two systems don't fight.
2026-09-22 2026-09-21 Verified NS ≥8.8 supports CSS custom properties, `calc`, media-query L3 (`prefers-color-scheme`, `orientation`, `min-width`), `matchMedia()`, `@keyframes` — theming/responsive vocabulary is genuinely shareable, not shimmed; strengthened CSS-first styling (decision #5).
2026-09-22 2026-09-21 User preference for design phase: map the entire system in markdown before any code; `docs/` holds our plan, `prior-art/` holds others' systems — plan vs precedent must never blur.
2026-09-22 2026-09-22 Git initialized per user approval (Annotation 1); doc tree committed — 47a1fd1 (landed) "docs: initial design docs for cross-platform Octane + NativeScript".
2026-09-22 2026-09-22 NS docs' "web" claim decomposes to non-UI TS code sharing + StackBlitz/Preview (browser hosts the bundler, a device still renders) — runtimes ship only as @nativescript/android|ios|visionos; no web target, doesn't apply to the Octane integration.
2026-09-22 2026-09-22 DOMiNATIVE (nativescript-community) implements a DOM-ish API inside the NS runtime producing native views — Solid's NS renderer builds on it; opposite direction, not web output.
2026-09-22 2026-09-22 Decision #17 rejected emulating @nativescript/core views over DOM (landed — d600a0c "docs: record what NativeScript's 'web' claim actually means and reject NS-view DOM emulation"): costs SSR, semantic HTML, real a11y/DOM events while Octane's DOM renderer is already fi
2026-09-22 2026-09-22 Framing landed: "we own the contracts, upstream owns the engines" — forced-to-own list ranked by blast radius: primitives contract, navigation contract, resolution toolchain, seam enforcement, animation/gesture facade, platform services, version matrix+patches.
2026-09-22 2026-09-22 Rejected a `docs/problems/` subfolder (existing docs already map 1:1 to owned problems — parallel tree would drift); landed d51270e tracking layer instead: `docs/README.md` dashboard + per-doc status headers, vocab `mapped→validated→building→built`.
2026-09-22 2026-09-22 User decision: Silo will track design-exploration progress; DB is greenfield (detached identity, state absent). Proposed schema tables: topics, questions, decisions, experiments + saved queries (open-by-blast-radius, blocked-topics).
2026-09-22 2026-09-22 Exploration ordering principles: blast-radius first (substrate verification before building design on it), desk before lab (source-reading answers most unknowns), contracts before mechanics (lint rules downstream of known seams).
2026-09-22 2026-09-22 Phase 1 desk plan: clone octanejs/octane, nativescript-community/octane, NathanWalker/ns-octane into a gitignored `research/` dir — source-reading expected to close ~8 top open questions (export delta, glob coverage, driver internals, HMR model).
2026-09-22 2026-09-22 Universal surface verified ≈ full React API — all hooks, `lazy`, `memo`, `createPortal`, `Activity`, transitions, `use()` — plus portal capability types, a transport protocol, `UniversalTextPolicy` ('reject'|'ignore'|'host'). No `<Suspense>` on universal → `@try`/`@pen
2026-09-22 2026-09-22 Decisions #18–#25 landed: `resolve.extensions`+`moduleSuffixes` gives leaf-suffix resolution with zero custom resolver code; the compiler's `validation` config is a free compile-time seam enforcer; `.tsrx` is auto-owned by extension (no glob check needed).
2026-09-22 2026-09-22 Text model seam: NS rich text is flat — one `formattedstring`, sibling spans — so nested styled `<Text>` can't nest as elements; inner spans absorb parent classes via text-context. Needs a small driver patch (Span auto-wrap, #text folding) — decision #25, patch-package
2026-09-22 2026-09-22 Design phase complete: 40/41 Silo topics resolved, `docs/spec.md` synthesis + `docs/css-support-matrix.md` landed (7 commits through 6279c4c); only `dev-loop-topology` stays queued — purely empirical, folded into the 8-experiment lab queue.
2026-09-22 2026-09-22 Harness feasibility confirmed: octane 0.4.0, `@nativescript-community/octane` 0.2.0, `@nativescript/core` 9.1.2 all published. Native half needs `npm i -g nativescript` + `ns doctor` (Xcode/Android runtimes) — paused for user OK on global toolchain install.
2026-09-22 2026-09-22 a5e6cff (landed) "fix(native): iOS render path — universal compile, dep resolution, pinned deps" — app boots on iPhone sim, App renders+mounts, taps re-render, ListView per-cell roots clean.
2026-09-22 2026-09-22 `octaneConfig(mode, options)` takes plugin options as the SECOND arg — a single-arg `{mode, octane:{…}}` call is silently ignored, so `.tsrx` compiles through the DOM renderer even when renderer rules match.
2026-09-22 2026-09-22 Linker reversal: hoisted `node_modules` breaks the native vendor path (`getExportsReverseMap` reads per-app `node_modules/<pkg>/package.json` → require misses → `{}`). Isolated + `preserveSymlinks:false` works; chokidar pinned 3.6.0 via overrides.
2026-09-22 2026-09-22 Vendored `__nsRequire` loses `export *` re-exports — in `.native.tsrx` leaves import universal hooks `from 'octane'` (compiler retargets to working served-ESM); `@nativescript-community/octane` only for own exports (`renderNativeScriptApp`, `createNativeScriptRoot`).
2026-09-22 2026-09-22 tsconfig `paths` is the native HMR-scope lever — watcher allowlists appPath + paths-alias roots; `packages/` needs explicit file-target mappings (dir wildcards fail). NSLog carries `(NativeScript) CONSOLE *` — the device observability channel.
2026-09-22 2026-09-22 All 8 lab experiments passed on iOS sim (ca674a6..ba06968 landed): cell roots, controlled input, two-server topology, prod tree-shake, ~1ms token swap, .ios>.native precedence, a11y props, seam validation.
2026-09-22 2026-09-22 NS ListView refires `itemLoading` on any layout-affecting render and rotates host→index per wave; `e.item` lags splices one wave — leaf dedups via module-scope maps keyed by native objects + `items[index]` + post-splice `refresh()`.
2026-09-22 2026-09-22 `useRef().current` writes land in draft hooks during render — pre-commit event closures read stale values; module-scope storage keyed by the native object avoids hook semantics entirely.
2026-09-22 2026-09-22 Stock `nativeScriptRenderer` ships zero `forbiddenGlobals` — the no-DOM seam needs a validation block in app config; it only checks compile-pipeline files (.tsrx), so plain .ts helpers stay unchecked (no-DOM-lib tsconfig backstops).
2026-09-22 2026-09-22 DOM octane modules pollute only the DEV deps-bundle (eager package-dep seeding); prod `ns build` vendor is clean. Device discovers the native vite port via synced app metadata — auto-bumps, no hardcoding.
2026-09-22 2026-09-22 User authorized upstream reporting ("report whatever you like, avoid bundling unrelated issues") — 5 single-issue reports filed: ns-community/octane #1 managed listview, #2 default forbiddenGlobals, #3 textChange echo; NativeScript #11440 dev deps-bundle DOM octane; oc
2026-09-22 2026-09-22 Deliberately not reported upstream: ListView host↔index rotation and `e.item` splice lag — judged NS core behavior, not driver bugs.
2026-09-22 2026-09-22 4a256e7 (landed) — links the 5 upstream issue reports from the findings docs sections they evidence.
2026-09-22 2026-09-22 User manually verified real typing + tapping on the iPhone sim (Annotation 3) — open-questions #4 updated; cursor-position and Android IME remain the open edges.
2026-09-22 2026-09-22 Design verdict recorded: priced risk is substrate youth → per-primitive leaf folklore; upstream absorption of driver fixes (or a thin owned driver-wrapper) is load-bearing framework scope, not optional citizenship.
