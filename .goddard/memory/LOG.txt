2026-09-22 Octane renderer ownership is per-file at compile time (`nativeScriptRenderers` include glob): a file speaks one element vocabulary, so leaf components must split into `*.web.tsrx` / `*.native.tsrx` — `Platform.select` can't mix intrinsics in one file's JSX.
2026-09-22 Hook-containing modules must live inside the renderer include glob (`.tsx`/`.tsrx`); hooks imported from plain `.ts` bind a second Octane runtime whose dispatcher never activates — the two-copies-of-React bug repackaged.
2026-09-22 Reference app: `NathanWalker/ns-octane` — ChatGPT-style client (streaming markdown, ui-drawer `translationFunction`, Tailwind v4 via `@nativescript/tailwind`, second Octane root in a `UISheetPresentationController`); its README reads as the port's design doc.
2026-09-22 Planned leaf-resolution chain `*.ios.tsrx` → `*.native.tsrx` → `*.tsrx` via Vite resolver plugin; needs two tsconfigs for per-target `jsxImportSource`; open check: whether the include glob covers `.tsrx`, not just `.tsx`.
2026-09-22 User named One (onestack.dev) and Flutter as prior art for the cross-platform primitives/styling layer; RN/RNW/Tamagui are the comparable precedents.
2026-09-22 2026-09-21 Modal is the sharpest web↔native seam: a native modal is a second Octane root (`showModal`), so context and portals don't cross — modal APIs must be `{open, params}`-shaped, treating content as a screen, not children.
2026-09-22 2026-09-21 `List` primitive takes `items`+`renderItem`, never children: NS `ListView` item templates don't reconcile like DOM children — locked in as decision #16.
2026-09-22 2026-09-21 NS toolchain already resolves `.ios.`/`.android.` filename suffixes natively; our leaf-resolution scheme is a superset and precedence must stay aligned so the two systems don't fight.
2026-09-22 2026-09-21 Verified NS ≥8.8 supports CSS custom properties, `calc`, media-query L3 (`prefers-color-scheme`, `orientation`, `min-width`), `matchMedia()`, `@keyframes` — theming/responsive vocabulary is genuinely shareable, not shimmed; strengthened CSS-first styling (decision #5).
2026-09-22 2026-09-21 User preference for design phase: map the entire system in markdown before any code; `docs/` holds our plan, `prior-art/` holds others' systems — plan vs precedent must never blur.
