# Decisions

> Ledger of design commitments. **Decided** = build to it. **Provisional** =
> build to it but flag for revisit at first friction. **Forced** = the platform
> decided for us. New entries append; reversals get a dated note, not an edit.

| #   | Decision                                                                                                                                                                                                    | Status                                 | Rationale                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Where                                     |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------- |
| 1   | One source tree compiled per target; never shared compiled output                                                                                                                                           | **Forced**                             | Renderer ownership is per-file at compile time                                                                                                                                                                                                                                                                                                                                                                                                                           | architecture                              |
| 2   | `*.web/native/ios/android.tsrx` suffix resolution via Vite resolver plugin                                                                                                                                  | Decided                                | RN/One convention; resolver also drives route tables + platform modules                                                                                                                                                                                                                                                                                                                                                                                                  | module-resolution                         |
| 3   | Leaf-vocabulary splits at file boundaries; no intrinsics in shared files                                                                                                                                    | **Forced**                             | A file compiles under exactly one renderer                                                                                                                                                                                                                                                                                                                                                                                                                               | architecture, primitives                  |
| 4   | Hook-calling code only in `.tsx`/`.tsrx` inside renderer include glob                                                                                                                                       | **Forced**                             | Hooks bind the runtime that owns the file                                                                                                                                                                                                                                                                                                                                                                                                                                | architecture                              |
| 5   | CSS-first styling: shared stylesheet + Tailwind + CSS-var tokens; style objects for dynamics only                                                                                                           | Provisional                            | NS has real CSS (+vars, media queries, keyframes) — unlike RN; avoid building a styling runtime                                                                                                                                                                                                                                                                                                                                                                          | styling                                   |
| 6   | Primitives shaped like RN's vocabulary (View/Text/Pressable), implemented per-target                                                                                                                        | Decided                                | RNW proves converging on the constrained surface works                                                                                                                                                                                                                                                                                                                                                                                                                   | primitives, prior-art/react-native-web    |
| 7   | `styled()` variants API over clsx composition                                                                                                                                                               | Provisional                            | Tamagui-shaped authoring, CSS-backed implementation                                                                                                                                                                                                                                                                                                                                                                                                                      | styling, prior-art/tamagui                |
| 8   | Shared route table + `_layout` shells; routes may platform-split                                                                                                                                            | Provisional                            | One's model; Frame/Page vs URL is irreconcilable below this seam                                                                                                                                                                                                                                                                                                                                                                                                         | navigation, prior-art/one                 |
| 9   | Modal = separate Octane root on native                                                                                                                                                                      | **Forced**                             | `showModal` hosts its own root; context/portal don't cross                                                                                                                                                                                                                                                                                                                                                                                                               | primitives, navigation                    |
| 10  | Animation facade (controller/`to`/`spring`), imperative writes, no worklets                                                                                                                                 | Provisional                            | JS is on the UI thread on NS — no bridge problem to solve                                                                                                                                                                                                                                                                                                                                                                                                                | animation-gestures                        |
| 11  | `packages/platform` = headless capability interfaces, resolved per-target                                                                                                                                   | Decided                                | standard seam; keeps DOM/NS APIs out of shared code                                                                                                                                                                                                                                                                                                                                                                                                                      | platform-services                         |
| 12  | Monorepo `apps/{web,native}` + `packages/*`                                                                                                                                                                 | Provisional                            | could start single-app; layout earns its cost with multiple targets/apps                                                                                                                                                                                                                                                                                                                                                                                                 | architecture                              |
| 13  | Named exports for components/routes                                                                                                                                                                         | Provisional                            | HMR accept-boundary hygiene (One/RN precedent)                                                                                                                                                                                                                                                                                                                                                                                                                           | toolchain                                 |
| 14  | Dark mode: `prefers-color-scheme` default + class override                                                                                                                                                  | Provisional                            | media queries exist on both targets                                                                                                                                                                                                                                                                                                                                                                                                                                      | styling                                   |
| 15  | pin exact versions; patch-package accepted                                                                                                                                                                  | **Forced-ish**                         | both upstreams moving; explicit version matrix required                                                                                                                                                                                                                                                                                                                                                                                                                  | toolchain                                 |
| 16  | No `FlatList`-style children API: lists take `items`+`renderItem`                                                                                                                                           | Decided                                | NS ListView item templates can't take reconciled children                                                                                                                                                                                                                                                                                                                                                                                                                | primitives                                |
| 17  | Web target = Octane's real DOM renderer, NOT NS views emulated over DOM                                                                                                                                     | Decided                                | No upstream DOM impl of `@nativescript/core` exists (NS "web" = code-sharing + browser-hosted tooling, Preview renders on device); emulation would also cost SSR, semantic HTML, a11y, real DOM events                                                                                                                                                                                                                                                                   | architecture, prior-art/nativescript-core |
| 18  | Suffix resolution via ordered `resolve.extensions` + TS `moduleSuffixes`; custom `resolveId` plugin only as fallback                                                                                        | Provisional                            | Octane plugin already extends `resolve.extensions` with `.tsrx`; resolution is filename-order; octane compiles by resolved filename so rule globs own `Foo.native.tsrx`. Verify mergeConfig array ordering; fall back to `resolveId` plugin if ordering can't be guaranteed                                                                                                                                                                                              | module-resolution                         |
| 19  | Portable async boundaries = `.tsrx` `@try`/`@pending`/`@catch`; `<Suspense>`/`<ErrorBoundary>` are DOM-only                                                                                                 | **Forced**                             | Universal runtime has `universalTry` + suspense machinery but exports no `Suspense` component; `.tsx` files needing boundaries get a shared `Boundary` wrapper or go `.tsrx`                                                                                                                                                                                                                                                                                             | architecture, primitives                  |
| 20  | Adopt compiler `renderers.*.validation` (`forbiddenGlobals`, `forbiddenImports`, `textHosts`, `textParents`, `hostProps`) as the first seam-enforcement layer                                               | Decided                                | Upstream machinery already validates renderer-owned files incl. `.ts` helpers — free, runs at compile not lint time                                                                                                                                                                                                                                                                                                                                                      | testing, architecture                     |
| 21  | `List` native leaf = ListView + per-cell `createNativeScriptRoot` sub-roots; `items` via `ObservableArray`; `itemLoading` rebinds recycled slots                                                            | Provisional                            | ListView's recycling contract is imperative (itemTemplate/itemLoading); per-cell roots are the react-nativescript-shaped answer. Lab: root-per-cell cost, templateSelector                                                                                                                                                                                                                                                                                               | primitives                                |
| 22  | `Overlay`/`Popover`/`Toast` on native = `RootLayout.open()` imperative bridge (own sub-root per overlay), app root renders `<rootlayout>`                                                                   | Provisional                            | Portals unimplemented in NS driver; RootLayout gives shadeCover + enter/exit animations + z-order in the main window                                                                                                                                                                                                                                                                                                                                                     | primitives                                |
| 23  | `.tsrx` as the default dialect for ALL renderer-owned files (shared + leaf + routes)                                                                                                                        | Provisional                            | `.tsrx` is auto-owned by extension (no pragma needed), directives (`@try` boundaries, `@for`) are the portable boundary/list syntax; `.tsx` reserved for files that must not see directives                                                                                                                                                                                                                                                                              | toolchain, module-resolution              |
| 24  | Bare text only inside text-hosting primitives (`Text`, `Button`, …); enforced via `validation.textHosts`/`textParents` + primitive contract                                                                 | Decided                                | Driver folds `#text` into `text` only when parent `instanceof TextBase`; text under a layout view is **silently dropped**                                                                                                                                                                                                                                                                                                                                                | primitives, testing                       |
| 25  | Nested `Text` needs two driver extensions: `Span` under `TextBase` auto-wraps into `formattedText`; `#text` under `formattedstring`/`span` folds to text                                                    | Provisional                            | Without them `<label><span/></label>` throws and mixed text+element children drop silently. ~40 lines; patch-package now, upstream PR candidate (completes `text:'host'` semantics)                                                                                                                                                                                                                                                                                      | primitives                                |
| 26  | Own the NS driver via a **fork** (`aleclarson/nativescript-octane`), not a wrapper layer                                                                                                                    | Decided                                | The seams found in Exp 1-8 are driver-internal (managed elements, prop-write semantics, event echo) — a wrapper cannot reach them; a fork can. Fork exists; pull upstream on releases, PR fixes back                                                                                                                                                                                                                                                                     | primitives, toolchain                     |
| 27  | Universal renderer **retains** unchanged-prop children (auto-memo); non-signal module-state readers subscribe via `useStore`                                                                                | **Forced** (upstream) + Decided (hook) | octane `universal-core` adopts committed subtrees wholesale on shallow-equal props + stable contexts — deliberate (scoped commits, replay bookkeeping); DOM re-invokes children React-style. Not fixable driver-side; the portable rule is per-reader subscription. _(2026-09-24 narrowed: `signal$`/`query$` `.get()` reads no longer count — universal signal reads subscribe the reading owner directly; validated on iOS sim in text-coral-ns, upstream PR pending)_ | primitives, architecture                  |
| 28  | Single-child NS hosts (`ContentView`/`Page`) never hold reconciled children — `Pressable`→`flexboxlayout`; imperative roots (Modal, Tabs panes, nav pushes, overlay/sheet, List cells) host on `GridLayout` | Decided                                | Driver assigns each child to `.content`, keeping only the last — siblings silently dropped (observed: tab label vanished under a conditional sibling). Grid children fill the cell, so single-child layout is unchanged. `className` arrays likewise comma-join dead via `String()` — leaves normalize with `cx()`                                                                                                                                                       | primitives                                |
| 29  | Native SVG uses `@nativescript-community/ui-svg` `SVGView`; `Icon` SVG/markup glyphs and `Image` SVG sources route to `svgview` (required peer) | Provisional | SVG is required. ui-svg provides inline markup, file/resource, and promise sources without adding a path parser. Native fidelity remains unverified; see Q21. | primitives, platform-services |
| 30  | Route presentations are declarative: `+modal`/`+fade` filename suffixes set the manifest default, `Route.presentation` overrides; modal = own root (showModal / web overlay), fade = push transition. `loader` exports are prefetch-only. Nested `_layout` chains wrap descendant routes on both leaves | Decided | Declarative defaults keep navigation data in the route dir where it belongs; prefetch (not a data layer) keeps the screen's query$ as the single source — iOS + web verified | navigation-notes |
